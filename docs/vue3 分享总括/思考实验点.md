# 一些点

1. watchEffect: 直接打印对象是不会触发更新的，必须是一些操作的处理。具体可看 Handler

2. setup 返回的值，要是响应式的。否则 DOM 无法更新。

3. toRefs: 支持后面解构。不使用时，如果监听对象的 get, set 还是可以做到的。

4. 每个属性方法，都有相应的类型定义。支持 Ts

5. markRow 如果作为响应式对象，是否可以报错提示。 或者其他考量。

6. 使用代理对象，过多的代理对象，会不会理解上，也会产生混淆，增加成本。细节上。